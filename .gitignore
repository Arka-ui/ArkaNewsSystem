# NEVER commit your private key
keys/private.pem
keys/*.key

# local python
__pycache__/
*.pyc
.venv/
venv/

# built API is regenerated by CI on every push; you can commit it if you want,
# but by default we let CI own it.
api/
